### Validación del campo Teléfono

A partir de la versión 1.1, el sistema valida que el campo "Teléfono" contenga únicamente números.
Si el usuario ingresa letras u otros caracteres, el sistema muestra un mensaje de error y no permite continuar con el registro.

